/**
 * Template for runtime-keys.ts (which is gitignored and holds real keys).
 *
 * For local development you can either:
 *   1) copy this file to `runtime-keys.ts` and paste your keys here, or
 *   2) leave keys empty and enter them at runtime in the app's Settings tab.
 *
 * These keys are shipped to the browser (client-side app), so treat this as a
 * local-demo convenience only — do not use it to embed secrets in a deployment.
 */
export const RUNTIME_KEYS = {
  geminiKey: '',
  mapsKey: '',
};
